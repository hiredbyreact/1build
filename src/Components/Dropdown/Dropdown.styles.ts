import { makeStyles } from '@material-ui/core/styles';

const Styles = makeStyles((theme) => ({
	ddMenu: {
		'& svg': {
			marginLeft: 5 + 16,
			color: '#7D8AB5',
		},
	},
}));

export default Styles;
