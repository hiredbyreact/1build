name: "Test typescript-action"
on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - run: yarn

      - uses: ./
        with:
          milliseconds: 1000
